# TODO: 埼玉県 中古マンション・戸建 相場マップ

---

## Phase 0-1: 準備〜MVP ✅ 完了

- [x] **T-001** 国交省CSVダウンロード（中古マンション 2024Q4-2025Q3）
- [x] **T-010** Python加工スクリプト作成（CSV→駅別集計→stations.ts）
- [x] **T-011** Nominatim Geocoding（162駅→148駅の座標取得）
- [x] **T-020** Next.js + React-Leaflet + Tailwind でフロントエンド構築
- [x] **T-021** マップ・フィルタ・エリアリスト・ホバーポップアップ実装
- [x] **T-022** 国交省実データ反映（7,625件 → 148駅）

## Phase 1後: 品質・信頼性改善（チームレビューより追加）

- [ ] **T-050** geocode_cache.jsonの座標精度を全駅目視確認、誤差駅を手動修正
- [x] **T-051** UIにデータ鮮度バッジを追加（「2025Q3時点のデータ」を常時表示）
- [ ] **T-052** Phase 3 AI機能にリクエストレートリミット実装（ユーザー/日単位）

---

## Phase 2: フィルタ強化 + 取引詳細

### 2A. 築年数マルチ選択 ✅ 完了

- [x] **T-100** FilterPanel: 築年数をドロップダウン → **トグルチップ（複数選択可）** に変更（チェックボックスより押しやすく、SP対応も見据えた`<button>`ベース）
- [x] **T-101** FilterState型変更: `ageCategory: AgeCategory` → `ageCategories: Set<AgeCategoryKey>`（空Set=すべて）
- [x] **T-102** utils.ts: 複数カテゴリの集計マージロジック追加（件数合計、価格は加重平均）
- [x] **T-103** StationMarkers / AreaList: マージ後のstatsで描画

### 2B. 面積切り替え ✅ 完了（フロントエンド計算で対応）

- ~~**T-110** データモデル変更: stations.ts を㎡単価ベースに変更~~ → **廃止**: `price70 / 70 * targetArea` のクライアント計算で完結。再生成不要。
- ~~**T-111** process_data.py: ㎡単価で集計するよう修正~~ → **廃止**: 同上。
- [x] **T-112** FilterPanel: 面積トグルボタン追加（50/60/70/80/90㎡）
- [x] **T-113** FilterState型変更: `targetArea: number` を追加（デフォルト70）
- [x] **T-114** utils.ts / 表示コンポーネント: `price70 / 70 * targetArea` でクライアント換算表示

### 2C. 取引データ一覧（駅詳細ドロワー）

- [x] **T-120** process_data.py: 駅別の個別取引JSONを `public/transactions/{code}.json` に出力（148駅分生成済み）
  - 各取引: 価格/面積/㎡単価/築年/間取り/構造/地区名/取引時期/徒歩分数
- [x] **T-121** StationDetail コンポーネント: 右側ドロワーUI（AI要約→数値サマリ→取引一覧の階層順）
- [x] **T-122** 駅マーカークリック / エリアリスト駅名クリック → ドロワーオープン
- [x] **T-123** 取引JSONの遅延読み込み（fetch on click、ローディング表示・キャンセル対応）
- [x] **T-124** 取引一覧のフィルタ連動（選択中の築年数でハイライト）+ カラムソート機能

### 2D. 過去データ追加 ✅ 完了

- [x] **T-130** 国交省CSVダウンロード: 中古マンション 2005Q3〜2024Q3（計6ファイル追加）
- [x] **T-131** process_data.py: 複数CSV対応（`sys.argv[1:]` で複数パスをマージ）
- [x] **T-132** constants.ts: YEARS を 2005〜2025 の21年分に拡張
- [x] **T-133** 動作確認: 76,412件 / 175駅 / 21年分で正常動作を確認

### 2E. 宅地（土地と建物）データ追加

- [ ] **T-140** 国交省CSVダウンロード: 宅地（土地と建物）2020〜2025
- [ ] **T-141** process_data.py: `--type house` オプション追加
  - 宅地用のフィルタ条件（土地面積 + 建物面積の扱い）
- [ ] **T-142** データモデル: `propertyType: "mansion" | "house"` を追加
- [ ] **T-143** FilterPanel: 物件種別タブ（マンション / 戸建）
- [ ] **T-144** stations.ts: 物件種別ごとに別データセットで管理

---

## Phase 3: AIサマリ + エリア深掘り

### 3A. AIエリア傾向分析 ✅ 完了

- [x] **T-200** Next.js API Route: `/api/summary` (Edge Runtime, Gemini 2.5 Flash ストリーミング)
- [x] **T-201** プロンプト設計: 直近5年推移 + 築年数別内訳 → 200字・自然文
- [x] **T-202** LLM連携: @google/genai SDK（Gemini 2.5 Flash）
- [x] **T-203** StationDetail: 生成ボタン + ストリーミング表示 + 24hキャッシュ
- [x] **T-204** `GOOGLE_API_KEY` を `.env.local` に設定・動作確認済み（Vercel環境変数はデプロイ時に設定）

### 3B. AIエリア提案（物件コンシェルジュ）✅ 完了

> **依存関係**: T-210以降はT-120（取引JSONファイル生成）が完了している必要がある。

- [x] **T-210** Next.js API Route: `/api/suggest` エンドポイント作成
- [x] **T-211** 条件解析プロンプト設計: 自然言語 → structured JSON（価格/築年数/面積/間取り等）
- [x] **T-212** データ照合ロジック: 解析された条件で全駅データをフィルタ・スコアリング
- [x] **T-213** 結果生成プロンプト設計: マッチ駅リスト → おすすめ理由テキスト
- [x] **T-214** SuggestPanel コンポーネント: 入力欄 + 提案カードUI（例文付き）
  - 各提案に「マップで見る・取引を確認する →」ボタン
  - クリックでStationDetailドロワーを開く
- [x] **T-215** マップ連携: 提案された駅をハイライト（青枠＋拡大）、他駅を透明化
- [x] **T-216** localStorageキャッシュ（同一条件は24h保持）

### 3C. 沿線別価格推移グラフ

- [ ] **T-220** データ加工: 沿線×年度の時系列集計
- [ ] **T-221** Chart.js or recharts でグラフコンポーネント作成
- [ ] **T-222** StationDetailまたは専用ページでグラフ表示

---

## Phase 8: UX改善・オンボーディング（第7回レビューより）

> 第7回チームレビュー（2026-03-17）で設計。G-XX 番号は `docs/09_チームレビューレポート_第7回` のアクションアイテムと対応。

### 8A. 面積拡張（前提作業）

- [x] **G-00** `src/lib/constants.ts`: `TARGET_AREAS` に `30, 40` を追加（`[30, 40, 50, 60, 70, 80, 90]`）
- [x] **G-14** 30㎡換算で83%が `under3000` に集中することを確認。G-12（PRICE_RANGES_SQM）と合わせて閾値調整予定

### 8B. 初回オンボーディングウィザード

- [x] **G-26** ウィザード STEP 3 のテキスト入力を `localStorage["wizard_area_text"]` に保存（G-25 の自動提案で参照）
- [x] **G-01** `src/components/OnboardingWizard.tsx` 作成。フルスクリーンモーダル、3ステップ、ステップインジケーター（●○○）
- [x] **G-02** STEP 1（予算設定）: 既存 BudgetPanel の年収・資産選択 UI を流用。免責文言追加
- [x] **G-03** STEP 2（世帯人数）: カードUI 3択。30/60/80㎡マッピング
- [x] **G-04** STEP 3（住みたいエリア・任意）: 自由テキスト + スキップボタン。予算自動付加 + `/api/suggest` 呼び出し
- [x] **G-05** `page.tsx` に `suggestQuery / suggestResult` を上位 state として追加。`SuggestPanel` に `initialResult / onResultChange` props を追加
- [x] **G-06** ウィザード完了時に `localStorage["onboarding_completed"]` と `budget_callout_dismissed` を保存
- [x] **G-08** `page.tsx`: タイトルを "埼玉県 中古マンション相場" に固定。他県表示中は "(X県表示中)" をサブラベルで表示（F-04 の代替）

### 8C. ㎡単価表示モード

- [x] **G-11** `FilterState` に `displayMode: "total" | "sqm"` を追加。FilterPanel に "総額 / 単価" トグルを追加。単価モード時は面積ボタンを `opacity-30 pointer-events-none`
- [x] **G-12** `constants.ts` に `PRICE_RANGES_SQM` を追加（〜35 / 35〜55 / 55〜80 / 80万円/㎡〜）。`getPriceRange / getPriceColor / getDisplayValue / formatDisplayValue` をモード対応に更新
- [x] **G-13** `StationMarkers` / `AreaList` / `StationDetail`: `displayMode === "sqm"` のとき `price70 / 70`（万円/㎡）を表示。各ラベル・ツールチップ文言もモードに応じて切り替え

### 8D. お気に入りエリア機能（Phase A）

- [x] **G-23** `page.tsx` に `favorites: Set<string>` state を追加。localStorage に `favorites` キーで保存。`StationDetail` ドロワーに ★ ボタン（登録・解除）。登録済みマーカーに ★ 表示
- [x] **G-24** `AreaList` 先頭に "⭐ お気に入りのエリア" セクションを追加（0件時は非表示）。2件以上になると "似たエリアを探す →" リンクを表示
- [x] **G-25** お気に入り登録時に `localStorage["wizard_area_text"]` + 駅コードで `/api/suggest` を自動実行。結果を `localStorage["favorites_suggest_cache"]` にキャッシュ（24h）。次回訪問時に即表示

### 8E. フィルター改善

- [x] **G-15** `FilterState` に `maxWalkMinutes: number | null` を追加。マーカーTooltipに徒歩分数を表示。※FilterPanel の駅徒歩トグルUIは Screen 2（/station/[code]）に移動する方針に変更（第8回レビューより）
- [x] **G-16** `visiblePriceRanges` トグルを FilterPanel 末尾で折りたたみ表示に変更（スペース節約）

### 8F. Screen 2（物件詳細ページ）設計

- [x] **G-19** Screen 2 実装: `app/station/[code]/page.tsx` 作成。価格サマリ・価格推移グラフ・AI傾向分析・取引一覧（徒歩分数列追加）・SUUMO外部リンク・類似エリア提案（予算超過時）。URLパラメータで area/mode/budget を引き継ぎ。StationDetailドロワーに「詳細ページで見る →」リンク追加
- [x] **G-20** フィルターの画面割り当て確定: Screen 1 = 予算・築年数・面積換算・㎡単価。Screen 2 = 徒歩分数（取引一覧列）・間取り（取引一覧列）・価格帯詳細

### 8G. 将来：エリアスコアリング準備（Phase 7C 先行設計）

- [ ] **G-21** エリアスコアリング軸の草案定義（飲食・子育て・治安・緑地・ビジネス・交通 + 人口構成・地価変動率）とデータソース整理
- [ ] **G-22** ライフスタイルプリセット定義と STEP 2 世帯人数との連動仕様（スコアデータ未整備のうちはプリセット定義のみ先行）
- [ ] **G-27** お気に入り Phase B: スコアデータ完成後、コサイン類似度ベースの高精度類似エリア計算に移行（G-25 の `/api/suggest` 代用を置き換え）
- [ ] **G-28** 複数お気に入り駅の横並び比較ページ `/compare?stations=code1,code2` の設計・実装

---

## Phase 4: デプロイ・公開

- [ ] **T-300** GitHub リポジトリ作成 + push
- [ ] **T-301** Vercel プロジェクト作成 + GitHub連携
- [ ] **T-302** 本番ビルド確認（SSG + 静的JSON）
- [ ] **T-303** OGP / メタ情報設定
- [ ] **T-304** 初回公開

---

## Phase 5: 予算ベースのエリア探索

### 5A. 予算シミュレーター

- [ ] **T-400** BudgetCalculator コンポーネント: 年収・資産入力 → 推定予算算出
  - ローン上限（年収×5〜8倍）+ 頭金（資産×10〜20%）で計算
  - 中央値 = (年収×6.5倍 + 資産×15%)
- [ ] **T-401** 年収×資産 マトリクス表の表示（リバベル参考）
  - 年収行（400/500/600/700/800/1000/1200/1500万）
  - 資産列（なし/300/500/1000/1500/2000/3000/5000万）
  - 現在値のセルをハイライト
- [ ] **T-402** /budget ページ作成 or トップにモーダルで配置
- [ ] **T-403** 「このエリアで買える駅を確認する →」ボタン → /?budget=XXXX に遷移

### 5B. 予算フィルタ連携

- [ ] **T-410** FilterState に `budgetMax: number | null` を追加
- [ ] **T-411** URLパラメータ `?budget=4000` でマップの予算フィルタを保持
- [ ] **T-412** マップ表示: 予算内=通常表示、予算外=グレーアウト（opacity 0.2）
- [ ] **T-413** フィルタパネルに「予算上限: X,XXX万円 ✕」バッジ表示
- [ ] **T-414** エリアリスト: 「予算内のみ / すべて表示」トグル

### 5C. 憧れエリア → 類似エリア提案

- [ ] **T-420** 予算外の駅クリック時に「憧れモード」を検出
- [ ] **T-421** 類似性スコアリングロジック（同一路線/隣接エリア/価格帯/築年数分布）
- [ ] **T-422** API Route: `/api/similar` — 憧れ駅 + 予算 → 類似かつ予算内の駅3-5つ
- [ ] **T-423** AIが「なぜ似ているか」「どう違うか」の解説を生成
- [ ] **T-424** StationDetail ドロワーに「○○が気になるあなたへ」提案セクション追加
- [ ] **T-425** 提案駅の [マップで見る] → 該当駅にフォーカス移動

---

## Phase 6: さらなる購入支援（将来）

- [x] **T-500** ハザードマップ重畳表示 ← 前倒し完了
- [ ] **T-501** 物件情報リンク（SUUMO/HOME'S等への外部リンク）
- [ ] **T-502** 不動産屋アポイント導線の検討
- [x] **T-503** レスポンシブ対応（SP）← 基本対応完了

---

## Phase 7: 関東エリア拡大 + ライフスタイル類似レコメンド

> **コンセプト**: 「埼玉4,000万の予算なら、他県のどこに住める？」「この駅と生活感が似てる駅は？」をデータで答える。
> まず関東1都6県（東京・神奈川・埼玉・千葉・茨城・栃木・群馬）から展開する。

### 7A. データ拡張（関東エリア）

- [ ] **T-600** 国交省CSVダウンロード: 関東1都6県の中古マンション取引データ（2020〜2025）
  - 都道府県コード: 13（東京）・14（神奈川）・11（埼玉、既存）・12（千葉）・08（茨城）・09（栃木）・10（群馬）
- [ ] **T-601** `process_data.py` に都道府県フィルタ・マージ対応を追加
  - 既存の埼玉データと関東データを統合。`prefectureCode` フィールド追加
  - 駅コードの重複対策（都道府県コードをプレフィックスに）
- [ ] **T-602** Nominatimジオコーディングを全関東駅に拡張（推定500〜700駅）
  - 既存の `geocode_cache.json` を流用・差分追加
- [ ] **T-603** `StationData` 型に `prefecture: string` フィールド追加
- [ ] **T-604** マップの初期表示範囲を関東全域に変更（中心: 東京駅付近）
  - 都道府県フィルタUIを追加（全関東 / 埼玉のみ 等）

### 7B. 都道府県をまたいだ予算比較

- [ ] **T-610** FilterPanelに都道府県トグルを追加（全選択デフォルト）
- [ ] **T-611** `?budget=4000&pref=saitama` のURLパラメータで都道府県絞り込みを永続化
- [ ] **T-612** 「この予算で買える駅マップ」ビュー
  - 予算設定時、全関東の予算内駅のみをハイライト表示
  - 「埼玉の予算内駅: X駅 / 東京: Y駅 / 神奈川: Z駅」のサマリパネル

### 7C. 駅の地域特性スコア算出

> **類似度の基準を人によってカスタマイズできる**のが重要。飲食店が多いか、保育園はあるか、治安はよいか、ビジネス街か、景観が良いか、など。

- [ ] **T-620** 地域特性データの収集・整形スクリプト作成
  - **データソース候補**（無料）:
    - 国土数値情報（MLIT）: 医療施設・学校・保育施設・公園・バス停等の施設POI
    - OpenStreetMap（Overpass API）: 飲食店・コンビニ・スーパー等の商業POI
    - e-Stat（政府統計）: 人口・年齢構成・外国人比率等のデモグラフィック
    - 警察庁オープンデータ: 犯罪発生件数（治安スコア）
  - **駅徒歩圏（半径1km）のPOI集計** → 駅ごとに特性スコアを生成
- [ ] **T-621** 駅ごとの特性スコアを `stations.ts` に追加
  ```typescript
  lifestyle?: {
    restaurants: number;    // 飲食店密度スコア (0-100)
    childcare: number;      // 保育園・幼稚園密度スコア (0-100)
    safety: number;         // 治安スコア (0-100)
    greenery: number;       // 公園・緑地密度スコア (0-100)
    business: number;       // オフィス・ビジネス施設密度スコア (0-100)
    transport: number;      // 交通利便性スコア (0-100)
  }
  ```
- [ ] **T-622** ライフスタイル重み付けUIコンポーネント（`LifestylePanel`）
  - 各軸のスライダー（0〜重視）
  - プリセット: 「子育て重視」「飲食・にぎわい重視」「静かな住宅街」「ビジネス利便」
  - リアルタイムで地図上のスコアに反映

### 7D. 類似エリアレコメンドAPI

- [ ] **T-630** `/api/similar-lifestyle` APIルート作成
  - リクエスト: `{ stationCode, weights: LifestyleWeights, budget?: number }`
  - コサイン類似度でライフスタイルスコアベクトルを比較
  - 予算フィルタ（任意）と組み合わせて類似かつ予算内の駅を返す
  - LLMが「なぜ似ているか」を説明（例: "どちらも飲食店が豊富で子育て施設も充実"）
- [ ] **T-631** `SimilarLifestylePanel` コンポーネント
  - 「この駅に似た雰囲気の駅を探す」ボタン from StationDetail
  - 類似駅カード（類似スコア + 価格差 + ライフスタイル比較レーダーチャート）
- [ ] **T-632** レーダーチャートコンポーネント（recharts `RadarChart` を使用）
  - 参照駅と類似駅のライフスタイル比較を可視化

### 7E. 「他県ではどこに住めるか」クロス県比較

- [ ] **T-640** 予算入力 → 全関東での「買える駅」一覧ページ or モーダル
  - 都道府県別の件数・代表駅・価格帯のサマリ
  - 「今見ている駅（例: 浦和）と似た生活感の駅を他県で探す」ボタン
- [ ] **T-641** AIによるクロス県レコメンド文生成
  - 例: 「浦和に似た雰囲気で予算4,000万以内なら、横浜市港北区（綱島・日吉）が近い特性を持ちながら予算内です」

---

## 優先順位とクリティカルパス

```
Phase 0〜5（埼玉MVP・AI機能・予算シミュレーター）✅ 完了
                    │
                    ▼
             Phase 4: デプロイ（進行中）
                    │
                    ▼
     ┌──────────────┴──────────────┐
     │                             │
Phase 6                       Phase 7A
物件リンク・不動産屋連携        関東エリアデータ拡張
（T-501/502）                 ← CSVダウンロード必要
     │                             │
     │                        Phase 7B
     │                        都道府県をまたいだ予算比較
     │                             │
     │                        Phase 7C
     │                        地域特性スコア算出
     │                        ← POIデータ収集が前提
     │                             │
     │                        Phase 7D/7E
     │                        類似エリアレコメンド
     │                        ← 7Cのスコア算出が前提
     └──────────────┬──────────────┘
                    ▼
              将来: 全国展開
```

**Phase 7 着手順序**:

1. **T-600〜604** 関東CSVダウンロード + データ統合（最初の壁はCSV収集）
2. **T-610〜612** 都道府県フィルタUI + 予算クロス比較
3. **T-620〜621** 地域特性スコア算出（POIデータ収集が最難関）
4. **T-622** ライフスタイル重み付けUI（スコアが揃ってから）
5. **T-630〜632** 類似エリアAPI + レーダーチャート
6. **T-640〜641** クロス県比較 + AIレコメンド文

**Phase 7 の前提・難所**:
- T-600: 関東のCSVは都道府県ごとに個別DLが必要（約6ファイル追加）
- T-620: POIデータ収集が最も工数がかかる。Overpass APIは無料だが集計スクリプト作成が必要
- T-621: 特性スコアの正規化・各軸の重み設定にドメイン知識が要る
- 関東全域で推定500〜700駅 → `stations.ts` のサイズが5〜10倍になる可能性 → API化を検討
